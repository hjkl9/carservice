syntax = "v1"

import "sms.api"
import "user.api"

type (
	PingReq struct{}
	PingRep struct{}
)

service carservice {
	@doc(
		summary: "check server status."
	)
	@handler Ping
	get /ping (PingReq) returns (PingRep)
}